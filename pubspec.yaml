name: litrato
description: A Flutter camera application

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  sqflite_sqlcipher: ^3.1.0+1
  path_provider: ^2.1.2
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
